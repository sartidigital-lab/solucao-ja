---
target: apps/admin
total_score: 26
p0_count: 0
p1_count: 2
timestamp: 2026-07-10T03-10-59Z
slug: apps-admin
---
⚠️ DEGRADED: single-context (spawn_agent unavailable in this session)

### Heuristics Scoring Table

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Real-time counts populate appropriately, but feedback on verification toggle needs loading spinners. |
| 2 | Match System / Real World | 3 | Terminology is professional and operational, but displays raw metadata IDs to normal operators. |
| 3 | User Control and Freedom | 3 | Direct actions on database tables lack a quick confirmation overlay. |
| 4 | Consistency and Standards | 2 | Shared type scale rules are violated; uses inconsistent custom LCH scopes and literal font sizing. |
| 5 | Error Prevention | 2 | Validation on dashboard forms is basic; lacks checking on provider input updates. |
| 6 | Recognition Rather Than Recall | 3 | Standard admin stats card dashboard, but state is not persisted upon tab navigation. |
| 7 | Flexibility and Efficiency | 3 | Functional layout for desktop, but has table truncation issues on smaller horizontal viewports. |
| 8 | Aesthetic and Minimalist Design | 2 | Cluttered rows with multiple nested tags and border + shadow card styling. |
| 9 | Error Recovery | 3 | Standard database error output displays, but errors can look overly technical. |
| 10 | Help and Documentation | 2 | Missing tooltips or guidelines explaining administrative permissions or user tags. |
| **Total** | | **26/40** | **Acceptable - Significant improvements needed** |

### Anti-Patterns Verdict

*   **LLM Assessment**: The dashboard has a functional grid structure, but contains visual markers of generic LLM-generated templates. This includes nested labels that force layout overflows and literal text sizes to pack too much content into grids, rather than utilizing clean pagination or responsive accordion lists.
*   **Deterministic Scan**: The automated design system detector identified:
    *   **9 Font size anomalies** targeting literal off-scale overrides (`text-[10px]` or `fontSize: 1.25rem`) across dashboard analytics cells, log inputs, and the authentication screen.
    *   **6 Color system drifts** in `globals.css` with raw LCH overrides like `oklch(0.45 0.14 145)` and `oklch(0.42 0.17 28)`.
*   **Visual Overlays**: Did not execute direct DOM screenshots. Automated CLI metrics were used as the authoritative guide.

### Overall Impression
The admin dashboard contains rich features for reviewing active provider schedules and credentials, but fails on visual consistency. Transitioning the custom LCH values back to primary brand tokens and replacing literal font steps will create a robust, production-grade console.

### What's Working
1.  **Tabular Data Layouts**: Active filters for categories and providers are structured well and have clear column splits.
2.  **Navigation Flow**: Route segregation for auth and admin panel components fits Turborepo conventions.

### Priority Issues
*   **[P1] Typographic Scale drift (10px overlays)**
    *   *Why it matters*: Readability is significantly degraded on compact or mid-sized monitors.
    *   *Fix*: Replace `text-[10px]` with standard `text-xs` (12px).
    *   *Suggested command*: `$impeccable typeset apps/admin`
*   **[P1] Custom OKLCH palette drift**
    *   *Why it matters*: The custom overrides conflict with the core project's base colors.
    *   *Fix*: Abstract color blocks into unified primary/secondary system variables.
    *   *Suggested command*: `$impeccable colorize apps/admin`
*   **[P2] Inline Font Styling**
    *   *Why it matters*: Breaks design maintainability.
    *   *Fix*: Replace inline styles with class utility tokens like `text-xl`.
    *   *Suggested command*: `$impeccable typeset apps/admin`
*   **[P2] Card Border & Drop Shadow conflict**
    *   *Why it matters*: Creates visual clutter and breaks flat-by-default rules.
    *   *Fix*: Strip off unnecessary shadows from cards that use fine card borders.
    *   *Suggested command*: `$impeccable layout apps/admin`

### Persona Red Flags

*   **Casey (Distracted Mobile User / Admin on Call)**: Attempting to approve a provider request on a mobile browser or tablet is difficult due to small tap triggers and dense table data truncation.
*   **Jordan (First-Timer)**: An operator logs in and is faced with a dense wall of raw data with no explanation of what specific fields (e.g., plan states or verification categories) do.

### Minor Observations
*   The tables lack responsive cell folding hooks.
*   Database logs are rendered without format highlights.

### Questions to Consider
*   Should we implement a quick search modal (CMD+K) to find providers and users instantly?
*   Should the dashboard default to listing active, unreviewed verification requests on load?
